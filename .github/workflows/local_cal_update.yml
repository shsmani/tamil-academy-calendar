name: Refresh Tamil Academy Calendar

on:
  schedule:
    # Runs every Sunday at 00:00 UTC (Adjust as needed)
    - cron: '0 0 * * 0'
  workflow_dispatch: # Allows manual trigger from GitHub UI

jobs:
  update-ics:
    runs-on: ubuntu-latest
    permissions:
      contents: write # Grants permission to commit changes back to the repo

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install requests beautifulsoup4

      - name: Run local calendar updater script
        run: python local_update_calendar.py

      - name: Commit and push updated calendar_test.ics
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          # git add calendar_test.ics
          git add .
          if git diff --staged --quiet; then
            echo "No changes to calendar.ics"
          else
            git commit -m "Automated update of calendar.ics"
            git push origin HEAD:${{ github.ref_name }}
          fi
